{
  "//": "Remote Config defaults for the Ask ALRT assistant. Deploy with: firebase deploy --only remoteconfig",
  "parameters": {
    "agent_enabled": { "defaultValue": { "value": "true" }, "valueType": "BOOLEAN" }
  },
  "parameterGroups": {},
  "conditions": []
}
